El problema es el orden de las rutas en groups.py:

ORDEN ACTUAL (INCORRECTO):
1. POST /{group_id}/invitations  <- Este captura "invitations" como group_id
2. GET /{group_id}/invitations
3. GET /invitations/by-code/{code}  <- Nunca se alcanza porque ya fue capturado arriba
4. GET /invitations/pending
5. POST /invitations/accept/{code}

ORDEN CORRECTO (debe ser):
1. GET /invitations/by-code/{code}  <- Rutas específicas primero
2. GET /invitations/pending
3. POST /invitations/accept/{code}
4. POST /{group_id}/invitations  <- Rutas con parámetros después
5. GET /{group_id}/invitations
6. DELETE /{group_id}/invitations/{invitation_id}

Necesito mover los endpoints de línea 862-1197 ANTES de la línea 661.
